global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'jenkins-rollback-webhook'

receivers:
  - name: 'jenkins-rollback-webhook'
    webhook_configs:
      - url: 'http://51.20.8.221:8080/generic-webhook-trigger/invoke?token=ROLLBACK_52E8B9_TOKEN'
        http_config:
          basic_auth:
            username: "work"
            password: '110b2ca35751d682cb5cd178abc11b23c6'
        send_resolved: false
